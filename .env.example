# Copia este archivo a .env (ya está en .gitignore, nunca se sube al repo).
# Valores en Supabase: Project Settings → API.

PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJ...

# La "service role key" NUNCA va aquí ni en ningún archivo del repo:
# tiene acceso total saltándose RLS. Solo se usa, si hace falta, desde
# scripts de servidor fuera de este proyecto.
